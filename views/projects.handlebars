<div class="container max col-xs-12 col-md-12 col-lg-12">
    <div class="middle container">

            {{!-- 2. The each helper can insert an array of data --}}

    <div class="row">
            {{#each projects}}
                <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 each-project">
                    <div class="projects hovereffect col-lg-4 col-md-4 col-sm-12 col-xs-12 each-project">

                        <img src="images/{{image}}"  style="width: 300px; height: 300px;" class="img-rounded img-responsive" alt="Cinque Terre">
                        <!--<div class="overlay">
                            <h2 class="title">{{project}}</h2>
                            <p>
                                {{#if description}}
                                    <a href="#">
                                        <abbr title="description"> <i class="glyphicon glyphicon-plus"></i></abbr>
                                    </a>
                                {{/if}}

                                {{#if github}}
                                    <a href="{{github}}">
                                        <abbr title="github">
                                            <i class="fa fa-github"></i>
                                        </abbr>
                                    </a>
                                {{/if}}

                                {{#if website}}
                                    <a href="{{website}}">
                                        <abbr title="website">
                                            <i class="glyphicon glyphicon-globe"></i>
                                        </abbr>
                                    </a>
                                {{/if}}
                            </p>

                        </div>
                        -->

                    </div>
                    <div class="description-of-projects col-lg-7 col-md-7 col-sm-12 col-xs-12">
                        <h3>{{project}}
                            {{#if github}}
                                <a href="{{github}}">
                                    <abbr title="github">
                                        <i class="fa fa-github"></i>
                                    </abbr>
                                </a>
                            {{/if}}
                            {{#if website}}
                                <a href="{{website}}">
                                    <abbr title="website">
                                        <i class="glyphicon glyphicon-globe"></i>
                                    </abbr>
                                </a>
                            {{/if}}
                        </h3>
                        <h4>{{date}} </h4>
                        <p class="description"> {{description}}</p>
                        <p class="description"> Dev. Team: {{team}}</p>
                    </div>
                </div>
            {{/each}}

    </div>
    </div>
</div>